/** @type {import('next').NextConfig} */
const nextConfig = {
  // Prepared for future i18n (Arabic RTL support)
  // i18n: {
  //   locales: ['fr', 'ar'],
  //   defaultLocale: 'fr',
  // },
};

module.exports = nextConfig;
